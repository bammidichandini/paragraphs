# use bellow command to install requirements

```
pip install -r requirement.txt
```

# use below commands to run server
```
python manage.py runsever
```

# run jupyter and test APIs
```
jupyter notebook
```